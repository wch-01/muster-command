# Copy this file to .env for local Docker Compose.
# For named environments, copy it to .env.dev or .env.prod and run:
# docker compose --env-file .env.dev up -d
# docker compose --env-file .env.prod up -d

DISCORD_BOT_TOKEN=
APPLICATION_ID=
DISCORD_CLIENT_SECRET=
ADMIN_DISCORD_USER_IDS=
STATE=development

DATABASE_URL=postgresql://bot:bot_password@postgres:5432/starbot?schema=public
BOT_TIMEZONE=UTC
SETUP_PORT=3000
ADMIN_ALLOWED_HOSTS=localhost,127.0.0.1,::1,housetalonpinas.tailbb76d4.ts.net
